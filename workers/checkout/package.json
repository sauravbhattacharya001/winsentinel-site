{
  "name": "@winsentinel/checkout-worker-stub",
  "version": "1.0.0",
  "private": true,
  "description": "DEPRECATED STUB. The live checkout worker is winsentinel-pro/packages/checkout-worker (deployed at checkout.winsentinel.ai). This folder keeps a fail-safe 410 stub only; no deploy script so it cannot clobber production.",
  "scripts": {
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
