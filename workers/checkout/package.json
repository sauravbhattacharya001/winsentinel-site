{
  "name": "@winsentinel/checkout-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^3.60.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "stripe": "^15.0.0"
  }
}
