# DEPRECATED STUB — DO NOT DEPLOY.
#
# The live checkout worker runs at https://checkout.winsentinel.ai and is
# maintained in the private winsentinel-pro repo:
#   winsentinel-pro/packages/checkout-worker/  (wrangler name: winsentinel-checkout)
#
# This directory only keeps a fail-safe stub (src/index.ts returns 410 Gone).
# The worker name here is DELIBERATELY NOT "winsentinel-checkout" so that a
# `wrangler deploy` from this folder can never overwrite the production worker
# (Cloudflare overwrites a deployed Worker by name). See src/index.ts for the
# full rationale.
name = "winsentinel-checkout-stub-do-not-deploy"
main = "src/index.ts"
compatibility_date = "2024-09-01"
